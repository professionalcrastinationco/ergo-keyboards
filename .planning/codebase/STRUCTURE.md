# Codebase Structure

**Analysis Date:** 2025-12-26

## Directory Layout

```
D:\APPS\KEYBOARDS\
├── .planning/          # Project planning documentation
│   └── codebase/      # Codebase analysis documents
├── index.html         # Main application (960 lines - all code)
├── layout.png         # Reference image of keyboard layouts
└── .env              # Environment variables (GitHub credentials)
```

## Directory Purposes

**.planning/codebase/**
- Purpose: Project planning and codebase documentation
- Contains: Analysis documents (STACK.md, ARCHITECTURE.md, etc.)
- Key files: Generated by codebase mapping
- Subdirectories: None

**Root Directory:**
- Purpose: All application files
- Contains: Main HTML file, assets, configuration
- Key files: `index.html` (the entire application)
- Subdirectories: `.planning/` only

## Key File Locations

**Entry Points:**
- `index.html` - Single entry point, entire application

**Configuration:**
- `.env` - GitHub credentials (GITHUB_USER, GITHUB_REPO, GITHUB_PERSONAL_ACCESS_TOKEN)
- No build configuration files

**Core Logic:**
- `index.html` lines 454-958 - All JavaScript logic
- `index.html` lines 7-402 - All CSS styling
- `index.html` lines 404-452 - HTML structure

**Testing:**
- Not applicable - No test files exist

**Documentation:**
- `.planning/codebase/*.md` - Generated codebase documentation
- No README.md or other user documentation

**Assets:**
- `layout.png` - Reference image showing keyboard layout

## Naming Conventions

**Files:**
- lowercase with extension: `index.html`, `layout.png`
- dot prefix for config: `.env`

**Directories:**
- lowercase with dots for hidden: `.planning`
- lowercase: `codebase`

**Special Patterns:**
- Single-file architecture (no module separation)
- Inline CSS and JavaScript (no separate files)

## Where to Add New Code

**New Feature:**
- Primary code: `index.html` inline `<script>` tag
- Styles: `index.html` inline `<style>` tag
- Tests: Not applicable (no testing infrastructure)

**New Layout Layer:**
- Add to `layouts` object in `index.html` (around line 456)
- Add tab button in HTML (around line 418)

**New Key Type:**
- Modify `createKey()` function in `index.html` (line 779)
- Add CSS styling in `<style>` block (lines 7-402)

**New Zoom Feature:**
- Modify zoom functions in `index.html` (lines 731-777)
- Update UI controls in HTML (lines 440-449)

**Documentation:**
- `.planning/codebase/` for technical docs
- Consider adding `README.md` at root level

## Special Directories

**.planning/**
- Purpose: Generated planning and analysis documents
- Source: Created by GSD workflow tooling
- Committed: Yes (recommended for project documentation)

---

*Structure analysis: 2025-12-26*
*Update when directory structure changes*
